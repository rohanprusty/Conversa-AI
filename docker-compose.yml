version: '3.8'

services:
  backend:
    build: ./Server
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      # Add your other backend env variables here if needed
      
  frontend:
    build: ./Client
    ports:
      - "8080:80"
    depends_on:
      - backend